import type { StandingsSnapshot } from "./types";

// League position needs a real table (see fixtures/sample-data.ts) — placeholder until that exists.
export const PLACEHOLDER_STANDINGS: StandingsSnapshot = { position: "3rd", points: 18, division: "Division One" };
